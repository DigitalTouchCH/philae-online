<h1>Patients</h1>

<% if @patients.any? %>
  <ul>
    <% @patients.each do |patient| %>
      <li><%= link_to "#{patient.first_name} #{patient.last_name} (#{patient.date_of_birth})", patient_path(patient) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No patients available.</p>
<% end %>
